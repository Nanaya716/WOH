<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jxc.dao.PurchaseRecordsDao">

    <resultMap type="com.jxc.entity.PurchaseRecords" id="PurchaseRecordsMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="productId" column="product_id" jdbcType="INTEGER"/>
        <result property="purchaseTime" column="purchase_time" jdbcType="TIMESTAMP"/>
        <result property="quantity" column="quantity" jdbcType="INTEGER"/>
        <result property="unitPrice" column="unit_price" jdbcType="NUMERIC"/>
        <result property="totalPrice" column="total_price" jdbcType="NUMERIC"/>
        <result property="supplierName" column="supplier_name" jdbcType="VARCHAR"/>
        <result property="purchaserName" column="purchaser_name" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="PurchaseRecordsMap">
        select
          id, product_id, purchase_time, quantity, unit_price, total_price, supplier_name, purchaser_name
        from purchase_records
        where id = #{id}
    </select>

    <select id="queryByProductId" resultMap="PurchaseRecordsMap">
        select
            id, product_id, purchase_time, quantity, unit_price, total_price, supplier_name, purchaser_name
        from purchase_records
        where product_id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="PurchaseRecordsMap">
        select
          id, product_id, purchase_time, quantity, unit_price, total_price, supplier_name, purchaser_name
        from purchase_records
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="productId != null">
                and product_id = #{productId}
            </if>
            <if test="purchaseTime != null">
                and purchase_time = #{purchaseTime}
            </if>
            <if test="quantity != null">
                and quantity = #{quantity}
            </if>
            <if test="unitPrice != null">
                and unit_price = #{unitPrice}
            </if>
            <if test="totalPrice != null">
                and total_price = #{totalPrice}
            </if>
            <if test="supplierName != null and supplierName != ''">
                and supplier_name = #{supplierName}
            </if>
            <if test="purchaserName != null and purchaserName != ''">
                and purchaser_name = #{purchaserName}
            </if>
        </where>
        limit #{pageable.offset}, #{pageable.pageSize}
    </select>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from purchase_records
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="productId != null">
                and product_id = #{productId}
            </if>
            <if test="purchaseTime != null">
                and purchase_time = #{purchaseTime}
            </if>
            <if test="quantity != null">
                and quantity = #{quantity}
            </if>
            <if test="unitPrice != null">
                and unit_price = #{unitPrice}
            </if>
            <if test="totalPrice != null">
                and total_price = #{totalPrice}
            </if>
            <if test="supplierName != null and supplierName != ''">
                and supplier_name = #{supplierName}
            </if>
            <if test="purchaserName != null and purchaserName != ''">
                and purchaser_name = #{purchaserName}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into purchase_records(product_id, purchase_time, quantity, unit_price, total_price, supplier_name, purchaser_name)
        values (#{productId}, #{purchaseTime}, #{quantity}, #{unitPrice}, #{totalPrice}, #{supplierName}, #{purchaserName})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into purchase_records(product_id, purchase_time, quantity, unit_price, total_price, supplier_name, purchaser_name)
        values
        <foreach collection="entities" item="entity" separator=",">
        (#{entity.productId}, #{entity.purchaseTime}, #{entity.quantity}, #{entity.unitPrice}, #{entity.totalPrice}, #{entity.supplierName}, #{entity.purchaserName})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into purchase_records(product_id, purchase_time, quantity, unit_price, total_price, supplier_name, purchaser_name)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.productId}, #{entity.purchaseTime}, #{entity.quantity}, #{entity.unitPrice}, #{entity.totalPrice}, #{entity.supplierName}, #{entity.purchaserName})
        </foreach>
        on duplicate key update
        product_id = values(product_id),
        purchase_time = values(purchase_time),
        quantity = values(quantity),
        unit_price = values(unit_price),
        total_price = values(total_price),
        supplier_name = values(supplier_name),
        purchaser_name = values(purchaser_name)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update purchase_records
        <set>
            <if test="productId != null">
                product_id = #{productId},
            </if>
            <if test="purchaseTime != null">
                purchase_time = #{purchaseTime},
            </if>
            <if test="quantity != null">
                quantity = #{quantity},
            </if>
            <if test="unitPrice != null">
                unit_price = #{unitPrice},
            </if>
            <if test="totalPrice != null">
                total_price = #{totalPrice},
            </if>
            <if test="supplierName != null and supplierName != ''">
                supplier_name = #{supplierName},
            </if>
            <if test="purchaserName != null and purchaserName != ''">
                purchaser_name = #{purchaserName},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from purchase_records where id = #{id}
    </delete>

</mapper>

